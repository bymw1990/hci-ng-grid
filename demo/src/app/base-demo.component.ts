import {SafeHtml} from "@angular/platform-browser";

import * as prism from "prismjs";

export class BaseDemoComponent {

  /*generateConfig(html: string, columns?: string, listeners?: string): SafeHtml {
    let safeHtml: SafeHtml = "<pre class=\"language-html\"><code class=\"language-html\">"
        + prism.highlight(html, prism.languages["html"])
        + "</code></pre>";
    if (columns) {
      safeHtml += "<pre class=\"language-html\"><code class=\"language-javascript\">"
          + prism.highlight(columns, prism.languages["javascript"])
          + "</code></pre>";
    }
    if (listeners) {
      safeHtml += "<pre class=\"language-html\"><code class=\"language-javascript\">"
          + prism.highlight(listeners, prism.languages["javascript"])
          + "</code></pre>";
    }

    return safeHtml;
  }*/

}